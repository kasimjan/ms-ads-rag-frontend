# Standalone Vite entry — used by `npm run dev`/`npm run build`.
# The repo root `index.html` doubles as a CDN-based preview so you can
# open it directly without installing anything. Vite uses the file
# below as its entry instead — point it at `src/main.jsx`.
